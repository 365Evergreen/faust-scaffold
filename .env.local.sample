## Sample environment variables for local builds and CI
# Do NOT include secrets here. Store secrets in GitHub Actions secrets instead.

# Public WordPress GraphQL endpoint (provide a URL your build can access)
# Defaulting to the public site used during development; change if needed.
NEXT_PUBLIC_WORDPRESS_URL=https://365evergreen.com

# Faust/WordPress preview secret (leave commented out in sample)
# FAUST_SECRET_KEY=

# Public site URL (used for sitemaps and canonical links)
NEXT_PUBLIC_SITE_URL=https://365evergreen.github.io/faust-scaffold/
